        let name = "Rabin"
        //string.length()
        console.log(name.length)

        //string.toUpperCase()
        console.log(name.toUpperCase())

        //String.toLowerCase()
        console.log(name.toLowerCase())

        //string.slice()
        console.log(name.slice(2,4))
            
        //string.replace()
        let name1 = "Rabin Dhakal"
        console.log(name1.replace("Dhakal", "Dada"))

        //string.concat()        
        let name2 = "Thor"
        console.log(name.concat(" is friend of ", name2))
        
        //string.trim()
        let friend = "   IronMan    "
        console.log(friend)
        console.log(friend.trim())
